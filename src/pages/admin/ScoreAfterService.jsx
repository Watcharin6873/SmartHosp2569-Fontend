import React from 'react'
import FormScoreAfterService from '../../components/admin/FormScoreAfterService';

const ScoreAfterService = () => {
  return (
    <div>
      <FormScoreAfterService />
    </div>
  )
}

export default ScoreAfterService
